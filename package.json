{
  "name": "poker-monorepo",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "cd server && rimraf dist",
    "build": "cd server && nest build",
    "heroku-prebuild": "echo This runs before Heroku installs dependencies && cd server && rimraf dist",
    "heroku-postbuild": "cd server && npm ci && nest build"
  },
  "dependencies": {
  }
}
